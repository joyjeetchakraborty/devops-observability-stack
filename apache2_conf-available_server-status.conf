<IfModule mod_status.c>
    ExtendedStatus On

    <Location "/server-status">
        SetHandler server-status
        Require local
        # Allow MONITORING_DOCKER_NETWORK_SUBNET
        Require ip <docker network subnet>
    </Location>
</IfModule>
